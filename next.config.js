/** @type {import('next').NextConfig} */

module.exports = {
    theme: {
      initialColorMode: 'light',
      light: {
        themeColor: '#F7F8FF',
      },
      dark: {
        themeColor: '#575556',
      },
    },
  };
